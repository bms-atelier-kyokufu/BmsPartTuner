---
adr-id: M-01
target-class: Unknown
status: open
---

# 波形比較（SIMD）の数理モデル・設計判断

## キャッシュされた音声データ2個の高速比較

- 対象クラス: FastWaveCompare

**数学的証明 (Mathematical Proof)**
ピアソン相関係数 $r$ の定義式を変形すると、以下のようになる:

$$
r = \frac{\sum(x_i - \bar{x})(y_i - \bar{y})}{\sqrt{\sum(x_i - \bar{x})^2} \sqrt{\sum(y_i - \bar{y})^2}}
  = \frac{1}{n} \sum_{i=1}^{n} \left( \frac{x_i - \bar{x}}{s_x} \right) \left( \frac{y_i - \bar{y}}{s_y} \right)
$$

※ $s_x, s_y$ は標準偏差。
この $\left( \frac{x_i - \bar{x}}{s_x} \right)$ はデータを正規化（標準化）していることを示す。

**【Phase 2の変換（事前正規化）】**
事前処理で波形を正規化:

$$
\hat{x}_i = \frac{x_i - \bar{x}}{\sqrt{\sum_{j=1}^{n}(x_j - \bar{x})^2}}
$$

これにより、ロード時に正規化された波形 $\hat{x}_i$ を事前計算しておくことで、比較処理（相関係数の計算）は単なるドット積に帰着する:

$$
r = \sum_{i=1}^{n} \hat{x}_i \cdot \hat{y}_i
$$

**【演算効率の改善】**

- 従来版: 5変数の累積（$\sum x, \sum y, \sum x^2, \sum y^2, \sum xy$）+ 複雑な除算
- Phase 2版: 1変数の累積（$\sum(x \times y)$）のみ
- 演算密度: **80%削減**

**設計判断 (Why this algorithm?)**

ピアソン相関係数による波形の形状比較を採用。音量差やDCオフセットに影響されず、波形の相似性のみを評価するためです。
また以下の特徴を担保しています。

- 音量差に影響されない（±3dB、±6dBでも波形が相似なら高相関）
- DCオフセットに影響されない（相関係数 = 1.0を維持）
- ノイズに対して適度にロバスト（0.01程度のノイズなら相関 > 0.98）
- 逆相を確実に検出（相関係数 = -1.0）

**非採用としたアプローチ (Rejected Alternatives / Anti-patterns)**

1. **RMS比較による事前足切りの廃止**:
   ピアソン相関は計算過程で正規化を含むため、本質的に音量差を無視します。RMSフィルタによる足切りはこの特性と矛盾します。また、複雑な条件分岐はSIMD最適化の妨げとなるため、全データに対して一気にSIMDで相関計算を行うアプローチを採用しました。
2. **ラグ補正の自動化廃止**:
   ラグ（開始位置のズレ）は音ゲーの演奏感に直結するため、あえて補正せず、長さの一致と位相の同期を厳密に求めることで品質を担保しています。
3. **SoA (Structure of Arrays) メモリレイアウトによる多重ペア同時比較**:
   L1/L2キャッシュヒット率を最大化するために、複数の比較対象波形をインターリーブして同時にSIMD比較するアプローチ（AoS -> SoA）を試みましたが、浮動小数点の計算順序の変更に伴う微小な丸め誤差が原因で既存の統合テストが失敗したため、品質保証を優先し **あえて実装しない（見送り）** 決定を下しました。

## O(1) 長さ差分ヒューリスティックによる事前枝刈り

- 対象クラス: FastWaveCompare

**数学的証明 (Mathematical Proof)**

波形データ $A$ と $B$ において、これらが事前に無音トリミング済みであるとする。
トリミング済みの波形長をそれぞれ $L_A$, $L_B$ とし、もし二つの波形が完全に一致する同一の楽器音（開始タイミングや無音部分の長さが違うだけ）であるならば、トリミング後の有効な音声区間の長さは等しくなるはずである。

$$
L_A \approx L_B
$$

したがって、差分 $\Delta L = |L_A - L_B|$ が一定の閾値 $\theta_{time}$ （例：0.1秒 = $0.1 \times f_s$ サンプル）より大きい場合、それらは明らかに異なる発音データであるとみなせる。

**設計判断 (Why this algorithm?)**
重いSIMD処理（ピアソン相関計算）を行う前に、波形の長さ（フレーム数）のみを比較して $O(1)$ で枝刈りを行います。もし長さが異なる波形を無理に比較し一部が一致したとしても、結局「はみ出た部分に音が鳴っている」ために最終的なRMSや相関の閾値を満たせません。この簡単な $O(1)$ チェックによって、計算時間をほぼ半減させる劇的なパフォーマンス向上をもたらしました。

## 波形検証クラス（SIMD最適化版）

- 対象クラス: WaveValidation

**数学的証明 (Mathematical Proof)**

**【決定係数（R²）】**

$$
R^2 = 1 - \frac{RSS}{DSS}
$$

1パス計算のための最適化式（分散の公式展開）:

$$
DSS = \sum x^2 - \frac{(\sum x)^2}{n}
$$

**【ピアソン相関係数（ρ）】**

$$
\rho = \frac{\sum((x - \bar{x})(y - \bar{y}))}{\sqrt{\sum(x - \bar{x})^2 \sum(y - \bar{y})^2}}
$$

1パス計算のための最適化式（共分散・分散の公式展開）:

$$
Cov(X,Y) = E[XY] - E[X]E[Y]
$$

$$
Var(X) = E[X^2] - (E[X])^2
$$

これにより、$\sum x, \sum y, \sum x^2, \sum y^2, \sum xy$ を1回のループで同時計算できる。

**【Phase 2最適化（正規化波形のドット積）】**
波形が平均0、ノルム1に正規化されている場合、ピアソン相関係数は以下のドット積となる:

$$
\rho = \sum_{i=1}^{n} \hat{x}_i \hat{y}_i
$$

**設計判断 (Why this algorithm?)**
音声波形の類似度判定において、決定係数（R²）とピアソン相関係数（ρ）の2つの指標を提供します。R²は波形の「完全一致度」を測るのに適し、ρは音量差に左右されない「形状の相似性」を測るのに適します。大量のサンプリングデータに対する比較をリアルタイムで行うため、これらをSIMD（`Vector<T>`）で並列化・最適化しています。

**非採用としたアプローチ (Rejected Alternatives / Anti-patterns)**

- **LINQの利用廃止**: 過去の実装ではLINQや複数パスのループを使用していましたが、キャッシュミスとメモリアロケーションがボトルネックとなりました。配列走査を1回にまとめる「1パスアルゴリズム」とSIMDによるベクトル化を採用し、スカラー計算比で3〜8倍の高速化を実現しました。
- **float型での全計算**: 累積値（Sum）が巨大になるとfloatの精度限界で桁落ちが発生するため、累積結果を用いた最終計算（割り算や平方根）はdouble型にキャストして行うように修正しました。
