# **C\#開発におけるCopilot運用指針と出力制限**

本指針は、C\#プロジェクトでのコード品質の均一化と、Git管理下でのトレーサビリティ向上を目的としています。

## **1\. 設計思想と判断基準（Why）**

### **1.1 型推論（var）の積極活用**

C\#で var を活用するのは、冗長な型記述を排除して変数の役割そのものに焦点を当てるためです。

### **1.2 日本語によるコミットログ**

開発チーム内でのコンテキスト共有速度を最大化するため、日本語を採用します。

## **2\. プロンプト処理と出力制限**

### **2.1 ドキュメント生成の制限**

AIエージェントは、利用者から明確な指示がない限り、外部解説書を勝手に作成してはいけません。

## **3\. XMLドキュメントコメント規約 (New)**

IDEのインテリセンス品質と実装の透明性を両立するため、以下の構造を厳守してください。

### **3.1 summary と remarks の使い分け**

* **\<summary\>**: 「何をするものか」を1〜2行で記述。IDEのポップアップに表示される主要情報。  
* **\<remarks\>**: 以下のような「詳細情報」を記述。  
  * **Why**: なぜその設計・アルゴリズムを採用したか。  
  * **How**: 内部的なロジック、数学的定義、計算式。  
  * **Responsibilities**: クラスの具体的な責務リスト。  
  * **Constraints**: 使用上の注意点、パフォーマンス特性（SIMD使用等）。

### **3.2 構造化タグの活用**

* 複雑な説明には \<para\>（段落）、\<list type="bullet"\>（リスト）を使用する。  
* 他クラスの参照には \<see cref="TypeName"/\> を使用し、リンクを構築する。  
* 数式や定義などは \<remarks\> 内に構造化して残すことで、将来のリファクタリング時の根拠とする。

### **3.3 適用基準**

* 全ての public および protected なメンバーには、最低限 \<summary\> を付与する。  
* 数学的、あるいはビジネスロジック的に複雑なコード（例：WaveValidation）には、必ず詳細な \<remarks\> を付与し、理論的根拠を明文化する。

## **4．ADRドキュメントとコードの紐付け (ADRAnchor)** (New)

コードと設計上の意思決定（ADR: Architecture Decision Record）の整合性を維持し、AI や開発者が変更箇所の設計意図を即座に追跡できるようにするため、以下のルールを厳守してください。

### **4.1 ADRAnchorAttribute の付与**
* 重要な設計、最適化、複雑なアルゴリズムを伴うクラスや主要な処理メソッドには、必ず `[ADRAnchor("ADR-ID", nameof(TargetName))]` 属性を付与して、対応する `docs/adr` 配下のドキュメントと紐付けてください。
  * 例：`[ADRAnchor("OPT-11", nameof(FastWaveCompare))]`
* 紐付け先の ADR ドキュメント（`docs/adr/**/*.md`）側にも、どのクラスと紐づいているかを明記してください。

### **4.2 リファクタリングや分割時の追従**
* クラスの名前変更、機能の分割、新規追加を行った場合は、ADR ドキュメントの紐付け（`[ADRAnchor]`）が最新の状態に保たれているか、あるいはドキュメント側の更新が必要ないかを必ず確認してください。
* ADR に関連する箇所のロジックを変更する際は、必ず関連する `docs/adr` 内のファイルを事前に参照し、設計意図（Why）に反していないか確認してください。


## **5．PowerShell等によるファイル破損（文字化け）の防止規約** (New)

AIエージェントがファイル内容の編集や新規作成を自律的に行う際、エンコーディングの不整合による文字化け（Mojibake）の再発を防止するため、以下のルールを厳守してください。

### **5.1 原則としてエージェント専用ツールの使用**
* ファイルの編集には、PowerShellのコマンド（`Get-Content`, `Set-Content`, `Out-File` 等）やスクリプトを用いず、必ずエージェントに提供されているファイル操作ツール（`replace_file_content`, `multi_replace_file_content`, `write_to_file` 等）を最優先で使用してください。

### **5.2 PowerShellでテキストファイルを操作する際の強制ルール**
どうしてもPowerShellを用いてテキストファイルを処理・出力する必要がある場合は、以下の制約を厳守してください。
1. **UTF-8エンコーディングの明示**:
   * Windows PowerShell 5.1（デフォルト環境）の `Get-Content` や `Set-Content` は、エンコーディングを指定しない場合、システムのANSIコードページ（日本語環境では Shift-JIS / CP932）としてファイルを処理します。UTF-8 (BOMなし) ファイルをそのまま読み書きすると、日本語文字が致命的に破損します。
   * そのため、読み込み・書き込みの双方で必ず `-Encoding UTF8` （または `utf8`）を明示してください。
   * 例: `Get-Content -Path "..." -Encoding UTF8`
2. **リダイレクト演算子の使用禁止**:
   * PowerShell 5.1における `>` や `>>` によるリダイレクトは、デフォルトで UTF-16LE エンコーディングになります。これにより、UTF-8ベースのリポジトリ内で文字コードの混在や破損を引き起こすため、ファイル書き出し時のリダイレクト演算子の使用は厳禁とします。代わりに `Set-Content -Encoding UTF8` や `Out-File -Encoding UTF8` を使用してください。
